import React, {useEffect} from "react";
import axios from 'axios';

export default function ExtraTicking(){

    useEffect(() => {
        //axios.get(`/extra-ticking/today`).then().catch().finally()
    },[]);

    return (
        <div>
            Extra tickings
        </div>
    );
}
